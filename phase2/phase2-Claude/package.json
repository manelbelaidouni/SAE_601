{
  "name": "voronoi-app",
  "version": "1.0.0",
  "description": "Visualiseur de diagramme de Voronoï",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
